h = float(input('Insira a altura da superficie : '))
l = float(input('Insira a largura da superficie: '))
print('a área da superficie é de {} \n serão necessários {} L de tinta para pinta-la'.format(h*l, h*l/2))